export function Header() {
  return (
    <header className="relative z-20 flex items-center px-6 h-14 border-b border-[var(--owl-border)] bg-[var(--owl-parchment)]/60 backdrop-blur-sm">
      <span className="text-sm font-semibold tracking-wide text-[var(--owl-brown-deep)]">
        THE NOCTURNAL ATHENAEUM
      </span>
    </header>
  );
}
