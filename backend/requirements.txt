--extra-index-url https://download.pytorch.org/whl/nightly/cu128
--pre

torch
torchvision
torchaudio
transformers>=4.51.0
huggingface-hub
accelerate
python-dotenv
bitsandbytes==0.49.*

# Server / API
fastapi>=0.95.0
uvicorn[standard]>=0.22.0

# Database (Supabase Postgres via session pooler)
sqlalchemy[asyncio]>=2.0
asyncpg

# Auth (Supabase — verifies JWTs, reads the user behind a token)
supabase

# Optional utilities
requests